dbt-proj:
  target: dev
  outputs:
    dev:
      type: postgres
      host: host.docker.internal
      user: postgres
      pass: postgres
      port: 5432
      dbname: postgres
      schema: postgres
